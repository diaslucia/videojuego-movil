import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    mainScreenInput: {
        marginVertical: 30,
        textAlign: "center",
        fontSize: 15,
    },
})

export default styles;